version: 0.2

phases:
  install:
    commands:
      - echo "Navigating to source directory..."
      - cd src
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - cd src
      - echo "Building application..."
      - npm run build
artifacts:
  files:
    - src/index.html
