version: 0.2

phases:
  install:
    commands:
      - echo "Navigating to source directory..."
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Building application..."
      - npm run build
artifacts:
  files:
    - index.html
